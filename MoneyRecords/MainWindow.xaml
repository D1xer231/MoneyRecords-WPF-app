<Window x:Class="MoneyRecords.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MoneyRecords" xmlns:viewmodels="clr-namespace:MoneyRecords.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        WindowStyle="None"
        Title="MainWindow" Height="700" Width="1200" MinHeight="500" MinWidth="800">
    <Grid>

        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF001121"/>
                <GradientStop Color="Black" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Columns -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Rows -->
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Left navigation bar -->
        <Grid Grid.Column="0" Grid.RowSpan="3">
            <Border Background="White"
                    CornerRadius="20"
                    Opacity="0.3"
                    Margin="10,20,10,10"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch">
            </Border>
        </Grid>

        <!-- Top middle bar -->
        <StackPanel Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2">
            <Border Background="White" CornerRadius="20" Opacity="0.3" Margin="10,20,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <TextBlock x:Name="DateTime_TextBlock" Text="localdate" Foreground="Gray" FontSize="20" FontFamily="/Resources/Roboto/Roboto-Regular.ttf#Roboto" Margin="20,10,0,6"/>
            </Border>
        </StackPanel>

        <!-- Top right bar -->
        <Grid Grid.Row="0" Grid.Column="3">
            <Border Background="White"
                    CornerRadius="20"
                    Opacity="0.3"
                    Margin="10,20,20,0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"/>
        </Grid>

        <!-- Welcome text -->
        <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"  Orientation="Horizontal" Margin="10,10,0,0">

            <TextBlock Text="Welcome back,"
                       FontSize="24"
                       FontFamily="/Resources/Roboto/Roboto-Regular.ttf#Roboto"
                       Foreground="Gray"
                       Margin="10,0,10,0"/>

            <TextBlock Text="{Binding UserName}"
                       FontSize="24"
                       FontFamily="/Resources/Roboto/Roboto-Regular.ttf#Roboto"
                       Foreground="White"/>

        </StackPanel>

    </Grid>
</Window>
